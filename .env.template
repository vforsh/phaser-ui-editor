# Used in Playwright E2E "attach to running Electron" mode (PW_E2E=1):
# sets the Chrome DevTools Protocol (CDP) port Electron exposes (default: 9222).
PW_E2E_CDP_PORT=9222

# The port used for the editor control WebSocket server (default: 17870).
EDITOR_CONTROL_WS_PORT=17870

# The URL of the dev server to load in Electron (e.g. http://localhost:5173).
# If not set, Electron will load the production build from the filesystem.
ELECTRON_RENDERER_URL=http://localhost:5173
