# Use legacy peer dependency resolution to handle peer dependency conflicts.
# dtsroll@1.4.1 requires vite@"5 || 6" but we use vite@7.3.0.
# Without this flag, npm ci fails with ERESOLVE error.
legacy-peer-deps=true
